name: check-relative-markdown-links
description: Runs check-relative-markdown-links on default settings
inputs:
  sha:
    required: false
    description: "SHA of the commit from which to install the tool"
    default: f904cd8dd1912d436704894454187eb421ae1464

runs:
  using: "composite"
  steps:
    - name: Check relative markdown links
      shell: sh
      env:
        sha: ${{ inputs.sha }}
      run: |
        curl -sSfL "https://raw.githubusercontent.com/anttiharju/check-relative-markdown-links/$sha/check-relative-markdown-links.bash" | bash -s -- run --verbose
