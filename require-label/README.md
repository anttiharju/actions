# require-label

Takes in a list of labels from [get-labels](../get-labels/) and compares it against the `name` input.

## Example

See [require-semver-label/action.yml](../require-semver-label/action.yml).
