name: Editorconfig-Checker
description: Runs Editorconfig-Checker on files tracked by Git.

runs:
  using: "composite"
  steps:
    - name: Run Editorconfig-Checker
      shell: sh
      run: npx editorconfig-checker $(git ls-files)
