output:
  - success
  - failure

pre-commit:
  parallel: true
  jobs:
    - name: Test run-shellcheck
      run: ./run-shellcheck/test.py
      glob: "run-shellcheck/*"

    - name: Prettier
      run: npx prettier --write .
      glob: "*{.md,.yml}"
      stage_fixed: true
